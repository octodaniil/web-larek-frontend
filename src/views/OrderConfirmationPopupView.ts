import { IOrderConfirmationPopupView } from "../types/index";

export class OrderConfirmationPopupView implements IOrderConfirmationPopupView {
  showConfirmationMessage(): void {
    // Реализация отображения сообщения о подтверждении заказа
  }
}
