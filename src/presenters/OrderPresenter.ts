import { IOrderPresenter, IOrder } from "../types/index";

class OrderPresenter implements IOrderPresenter {
  // Метод для оформления заказа
  submitOrder(order: IOrder): void {

  }
}

export default OrderPresenter;
